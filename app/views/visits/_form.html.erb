<%= form_with(model: visit) do |form| %>
  <% if visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(visit.errors.count, "error") %> prohibited this visit from being saved:</h2>

      <ul>
        <% visit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pacient_id %>
    <%= form.select :pacient_id, Pacient.all.map { |p| [p, p.id]  }%>
  </div>

  <div class="field">
    <%= form.label :doktor %>
    <%= form.select :doktor_id, Doktor.all.map { |d| [d, d.id]  } %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
